// 한줄로 된 간단한 에디터 구현 영어 소문자만

#include <bits/stdc++.h>
using namespace std;

int main(void){
  ios::sync_with_stdio(0);
  cin.tie(0);
  list<int> L;
  string str, act1, act2;
  int n, cursor = 0;
  cin >> str;
  for (auto c :str) L.push_back(c); // 연결리스트에 저장
  cin >> n;
  cursor = 
  for (int i = 0; i<n; i++){
    cin >> act1;
    if(act1 = 'P'){
      cin >> act2;
      L.insert
    }
    
  }

}